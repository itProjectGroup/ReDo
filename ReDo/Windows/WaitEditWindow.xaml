<Window x:Class="ReDo.Windows.WaitEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit wait" Width="260" Height="120" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" WindowStyle="ToolWindow">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Wait time (milliseconds):" Margin="0,0,0,8"/>
        <TextBox x:Name="TbMilliseconds" Grid.Row="1" Padding="6,4" Margin="0,0,0,12"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="70" Height="28" Margin="0,0,8,0" IsDefault="True" Click="Ok_OnClick"/>
            <Button Content="Cancel" Width="70" Height="28" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
